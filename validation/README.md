# Verification observations

`report.json` records completed development commands and the final packaging
checks. The command counts are observations from separate runs, not generated
JUnit from a final combined run. No current coverage result is claimed.

The final combined setup was blocked by automatic approval review because the
account usage limit was reached. See `VALIDATION.md` for tested boundaries and
outstanding acceptance. Run `scripts/verify.py` on your configured environment
to produce fresh raw logs, native evidence, JUnit and coverage under
`build/verification/`.
